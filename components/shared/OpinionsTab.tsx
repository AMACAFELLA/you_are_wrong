interface Props {
    currentUserId: string;
    accountId: string;
    accountType: string;
}

const OpinionsTab = async ({ currentUserId, accountId, accountType}: Props) => {
    // Fetch profile opinions 
    
    return (
        <section>
            OpinionsTab
        </section>
    )
}

export default OpinionsTab;